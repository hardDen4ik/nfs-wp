<?php 

/* function dynamic_block_agni_instagram( $attributes, $content = '' ){

    extract($attributes);
    
    $countdown_classes = array(
        'agni-block-instagram',
        'agni-sale-countdown',
        !empty($timerIcon) ? 'has-timer' : '',
        'style-' . $displayStyle,
        $customClassName
    );

    ob_start();
    ?>
    
    return 'Instagram feed will go here!';
    <?php

    return ob_get_clean();
} */

?>